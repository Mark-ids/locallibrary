<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qathafiii.locallibrary">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/lauchericon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.CategoriesActivity" />
        <activity android:name=".screens.SignInActivity" />
        <activity android:name=".screens.RegisterActivity" />
        <activity android:name=".screens.BookActivity" />
        <activity android:name=".screens.ManageActivity" />
        <activity android:name=".screens.ManageCategoryBooksActivity" />
        <activity android:name=".screens.CategoryBooksActivity"/>
    </application>

</manifest>